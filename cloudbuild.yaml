# cloudbuild.yaml
options:
  logging: CLOUD_LOGGING_ONLY

steps:
  # Step 1: Build the Docker image used for testing and deployment
  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - build
      - -t
      - europe-west1-docker.pkg.dev/long-octane-477515-k6/cloud-run-source-deploy/stay-and-trek-service:$COMMIT_SHA
      - .
    id: BuildImage

  # Step 2: Run Django automated tests inside the built application image.
  # If tests fail here, Cloud Build stops and deployment is blocked.
  - name: 'gcr.io/cloud-builders/docker'
    id: RunDjangoTests
    entrypoint: 'bash'
    args:
      - '-c'
      - |
          echo "Running Django automated tests..."
          docker run --rm \
            -e SECRET_KEY=test-secret-key \
            -e DEBUG=0 \
            -e DJANGO_SETTINGS_MODULE=webmapping_project.settings_ci \
            -e SPATIALITE_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu/mod_spatialite.so \
            europe-west1-docker.pkg.dev/long-octane-477515-k6/cloud-run-source-deploy/stay-and-trek-service:$COMMIT_SHA \
            python manage.py test trails_api.tests.test_models trails_api.tests.test_filters trails_api.tests.test_api trails_api.tests.test_views trails_api.tests.test_integration trails_api.tests.test_security -v 2

  # Step 3: Push the Docker image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - push
      - europe-west1-docker.pkg.dev/long-octane-477515-k6/cloud-run-source-deploy/stay-and-trek-service:$COMMIT_SHA
    id: PushImage

  # Step 4: Deploy the new image to Cloud Run
  # This deployment uses the dedicated service account and Cloud SQL socket connection
  # NOTE: Using Cloud SQL Socket (unix domain socket) for secure database access via --add-cloudsql-instances
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    args:
      - gcloud
      - run
      - deploy
      - stay-and-trek-service
      - --image
      - europe-west1-docker.pkg.dev/long-octane-477515-k6/cloud-run-source-deploy/stay-and-trek-service:$COMMIT_SHA
      - --region
      - europe-west1
      - --add-cloudsql-instances=long-octane-477515-k6:europe-west1:stay-trek-db
      - --service-account
      - cloud-run-service-account@${PROJECT_ID}.iam.gserviceaccount.com
      - --remove-env-vars=SECRET_KEY,OPENWEATHERMAP_API_KEY
      - --update-env-vars=ACTIVE_DB=new,NEW_DB_NAME=stay_and_trek,NEW_DB_USER=postgres,NEW_DB_HOST=/cloudsql/long-octane-477515-k6:europe-west1:stay-trek-db
      - --update-secrets=MY_API_KEY=api_key_secret:latest,SECRET_KEY=django_secret_key:1,OPENWEATHERMAP_API_KEY=openweathermap_api_key:1,NEW_DB_PASSWORD=cloud_sql_password:latest
      - --allow-unauthenticated
    id: DeployToCloudRun
    env:
      - 'CLOUDSDK_CORE_PROJECT=$PROJECT_ID'

images:
  - europe-west1-docker.pkg.dev/long-octane-477515-k6/cloud-run-source-deploy/stay-and-trek-service:$COMMIT_SHA

# Substitutions (optional, but good practice for project ID)
# default_subs:
#   _REGION: europe-west1
